apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort # grants external access on port 30007
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30007
