apiVersion: v1
kind: Service
metadata:
  name: lms-service
spec:
  type: NodePort # grants external access on port 30007
  selector:
    app: lms
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30009
