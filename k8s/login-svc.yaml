apiVersion: v1
kind: Service
metadata:
  name: login-service
spec:
  type: NodePort # grants external access on port 30007
  selector:
    app: login
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30008
